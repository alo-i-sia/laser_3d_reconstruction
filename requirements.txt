# 单USB双目激光三维重建系统依赖
# Single USB Stereo Laser 3D Reconstruction System Requirements
# Python 3.8+ 推荐

# ==================== 核心依赖（必需） ====================
numpy>=1.19.0,<2.0.0
opencv-python>=4.5.0
scipy>=1.7.0
numba>=0.53.0

# ==================== 点云处理（推荐） ====================
# Open3D用于高级点云操作和可视化
open3d>=0.13.0

# ==================== 开发工具（可选） ====================
# pytest>=6.0.0
# matplotlib>=3.3.0

# ==================== Jetson Nano特别说明 ====================
# Jetson平台可能需要从源码编译某些包
# 推荐使用JetPack SDK预装的包

# NumPy - 使用系统预装版本
# OpenCV - 使用JetPack预装的opencv-python

# Open3D在Jetson上需要特殊编译:
# 1. 可以不安装Open3D，系统会自动降级到基础点云处理
# 2. 或者使用预编译wheel:
#    pip3 install open3d==0.13.0

# ==================== 注意事项 ====================
# 1. 本项目不依赖pyrealsense2（已移除RealSense支持）
# 2. Numba在某些ARM平台可能需要额外配置
# 3. 建议使用虚拟环境避免依赖冲突